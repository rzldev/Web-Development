<%- include("partials/header") -%>

<div class="post">
  <% listPost.forEach(function(post) { %>
  <h1><%= post.title %></h1>
  <p><%= post.body.substring(0, 100) %>... <a href="/post/<%= post.title %>">Read More</a></p>
  <% }); %>
</div>

<%- include("partials/footer") -%>
